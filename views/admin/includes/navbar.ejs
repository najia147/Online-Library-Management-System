<nav class="navbar navbar-expand-lg navbar-dark top-navbar sticky-top">
    <a class="navbar-brand" href="/" target="_blank">
        <img src="/img/book-logo.png" alt="Site Logo">
        &nbsp;
        E-Library &nbsp;&nbsp;&nbsp;
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if(isAuthenticated){ %>
        <li class="nav-item <% if(activeLink == 'book-inventory'){ %> active <% } %>">
          <a class="nav-link" href="/admin/book-inventory">Book Inventory</a>
        </li>
        <li class="nav-item <% if(activeLink == 'category'){ %> active <% } %>">
          <a class="nav-link" href="/admin/category">Category</a>
        </li>
        <li class="nav-item <% if(activeLink == 'author'){ %> active <% } %>">
          <a class="nav-link" href="/admin/author">Author</a>
        </li>
        <% } %>
      </ul>
      <ul class="navbar-nav">
        <% if(isAuthenticated){ %>
          <li class="nav-item <% if(activeLink == 'login'){ %> active <% } %>">
            <form action="logout" method="POST" class="d-inline">
              <input type="hidden" name="_csrf" value="<%=csrfToken%>">
              <input type="submit" class="nav-link logout-btn" value="Logout">
            </form>
          </li>
        <% } else { %>
        <li class="nav-item <% if(activeLink == 'login'){ %> active <% } %>">
          <a class="nav-link" href="/admin/">Login</a>
        </li>
        <% } %>
        <li class="nav-item <% if(activeLink == 'signup'){ %> active <% } %>">
            <a class="nav-link" href="/admin/signup">Sign Up</a>
          </li>
      </ul>
    </div>
  </nav>